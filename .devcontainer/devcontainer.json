{
    "name": "Starter",
    "build": {
        "dockerfile": "Dockerfile"
    },
    "postStartCommand": "bash -c 'curl -s https://ascii.art/random && curl -s https://api.quotable.io/random | jq -r \".content + \\\" - \\\" + .author\"'"
    
}
